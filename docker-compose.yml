services:
  db:
    image: pgvector/pgvector:pg17
    restart: unless-stopped
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: demediator
      POSTGRES_PASSWORD: demediator
      POSTGRES_DB: demediator
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
